import React from 'react';
import { PlottingData } from '@/hooks/usePlottingData';

type Props = {
    data: PlottingData,
};

export function TabbedVisualization(props: Props) {
    return <div>I am a tabbed viz</div>;
}
