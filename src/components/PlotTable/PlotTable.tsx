import React from 'react';
import { PlottingData } from '@/hooks/usePlottingData';

type Props = {
    data: PlottingData,
};

export function PlotTable(props: Props) {
    return <div>I am a plot table</div>;
}
